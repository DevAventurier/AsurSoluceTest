from api.models import CustomUser, Agent, Driver, Criteria, Almond, ControlPoint, ControlPointByAgent, RoadControl, CriteriaByControl, Vehicle

def clean_data_base():
    # # Supprimer tous les enregistrements dans chaque modèle
    # CustomUser.objects.all().delete()
    # Agent.objects.all().delete()
    Driver.objects.all().delete()
    # Criteria.objects.all().delete()
    # Almond.objects.all().delete()
    # ControlPoint.objects.all().delete()
    # ControlPointByAgent.objects.all().delete()
    # RoadControl.objects.all().delete()
    # CriteriaByControl.objects.all().delete()
    # Vehicle.objects.all().delete()

    